package RepeatedIntervwQ;

import java.util.LinkedList;

public class FindDuplicateArrayElewithPreInsertionOrder {


			public static void main(String[] args) {
				int [] arr= {1,8,2,1,7};
				int n=arr.length;
				// create Set collection to get only unique char
				LinkedList<Integer> set = new LinkedList<Integer>();
				
				/*for(int i=0; i<arr.length; i++)
				{
					set.addAll(n, set); 
				}
				
				//compare each character of set with all char og given string
				for (Integer no : set) {
					int count=0;
					for(int i=0; i<arr.length; i++)
					{
						if(no==arr.length)
						{
							count++;
						} 
					}
					//print char and count
					if(count>1)
					{
						System.out.println(no+ " = "+count);
					}
				}
			}

		}*/
